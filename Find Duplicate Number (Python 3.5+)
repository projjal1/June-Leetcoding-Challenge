class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        d=dict()
        for each in nums: 
            if each not in d:
                d[each]=1
            else:
                return each
